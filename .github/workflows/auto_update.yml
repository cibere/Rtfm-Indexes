# this is automatically generated by
# https://github.com/cibere/Rtfm-Indexes/blob/master/.github/update_workflow_builder.py

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Print Option
      run: echo ${{ inputs.indexer }}
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'tom-select.js.org' }}
      name: index tom-select.js.org
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: tom-select.js.org
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'github.com' }}
      name: index github.com
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: github.com
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'discord.sex' }}
      name: index discord.sex
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: discord.sex
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'flowlauncher.com' }}
      name: index flowlauncher.com
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: flowlauncher.com
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'lua.org' }}
      name: index lua.org
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: lua.org
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'docs.qmk.fm' }}
      name: index docs.qmk.fm
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: docs.qmk.fm
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'voidtools.com' }}
      name: index voidtools.com
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: voidtools.com
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'autohotkey.com' }}
      name: index autohotkey.com
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: autohotkey.com
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'developer.mozilla.org'
        }}
      name: index developer.mozilla.org
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: developer.mozilla.org
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'discord.dev' }}
      name: index discord.dev
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: discord.dev
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'docs.github.com' }}
      name: index docs.github.com
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: docs.github.com
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'discord.com' }}
      name: index discord.com
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: discord.com
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'docs.discord.sex' }}
      name: index docs.discord.sex
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: docs.discord.sex
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'mpv.io' }}
      name: index mpv.io
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: mpv.io
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'ss64.com' }}
      name: index ss64.com
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: ss64.com
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'mathworks.com' }}
      name: index mathworks.com
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: mathworks.com
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'docs.datasecurity.org'
        }}
      name: index docs.datasecurity.org
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: docs.datasecurity.org
    - if: ${{ inputs.indexer == 'all' || inputs.indexer == 'developer.hashicorp.com'
        }}
      name: index developer.hashicorp.com
      uses: cibere/Rtfm-Indexes@run-indexer-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: developer.hashicorp.com
    - continue-on-error: true
      name: Push Changes
      run: 'git add indexes_v2

        git stash

        git fetch

        git switch indexes-v2

        git stash pop 0

        git add indexes_v2

        git -c user.name="github-actions[bot]" -c user.email="41898282+github-actions[bot]@users.noreply.github.com"
        commit --author="cibere <71997063+cibere@users.noreply.github.com>" -m "Auto
        Update Indexes"

        git push'
name: Auto Update
'on':
  schedule:
  - cron: 23 23 * * *
  workflow_dispatch:
    inputs:
      indexer:
        default: all
        description: the indexer to run
        options:
        - all
        - tom-select.js.org
        - github.com
        - discord.sex
        - flowlauncher.com
        - lua.org
        - docs.qmk.fm
        - voidtools.com
        - autohotkey.com
        - developer.mozilla.org
        - discord.dev
        - docs.github.com
        - discord.com
        - docs.discord.sex
        - mpv.io
        - ss64.com
        - mathworks.com
        - docs.datasecurity.org
        - developer.hashicorp.com
        required: false
        type: choice
