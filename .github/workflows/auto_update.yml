# this is automatically generated by
# https://github.com/cibere/Rtfm-Indexes/blob/master/.github/update_workflow_builder.py

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'lua.org' }}
      name: index lua.org
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: lua.org
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'flowlauncher.com' }}
      name: index flowlauncher.com
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: flowlauncher.com
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'voidtools.com' }}
      name: index voidtools.com
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: voidtools.com
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'docs.discord.sex' }}
      name: index docs.discord.sex
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: docs.discord.sex
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'autohotkey.com' }}
      name: index autohotkey.com
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: autohotkey.com
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'docs.datasecurity.org' }}
      name: index docs.datasecurity.org
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: docs.datasecurity.org
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'developer.mozilla.org' }}
      name: index developer.mozilla.org
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: developer.mozilla.org
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'mpv.io' }}
      name: index mpv.io
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: mpv.io
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'discord.sex' }}
      name: index discord.sex
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: discord.sex
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'discord.dev' }}
      name: index discord.dev
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: discord.dev
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'ss64.com' }}
      name: index ss64.com
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: ss64.com
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'tom-select.js.org' }}
      name: index tom-select.js.org
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: tom-select.js.org
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'docs.qmk.fm' }}
      name: index docs.qmk.fm
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: docs.qmk.fm
    - if: ${{ inputs.parser == 'all' || inputs.parser == 'discord.com' }}
      name: index discord.com
      uses: cibere/Rtfm-Indexes@run-parser-action
      with:
        error_hook: ${{ secrets.INDEX_ERROR_HOOK_URL }}
        filename: discord.com
    - continue-on-error: true
      name: Push Changes
      run: 'git fetch

        git pull

        git add indexes_v2

        git -c user.name="github-actions[bot]" -c user.email="41898282+github-actions[bot]@users.noreply.github.com"
        commit --author="cibere <71997063+cibere@users.noreply.github.com>" -m "Auto
        Update Indexes"

        git push'
name: Auto Update
'on':
  schedule:
  - cron: 23 23 * * *
  workflow_dispatch:
    inputs:
      parser:
        default: all
        description: the parser to run
        options:
        - all
        - lua.org
        - flowlauncher.com
        - voidtools.com
        - docs.discord.sex
        - autohotkey.com
        - docs.datasecurity.org
        - developer.mozilla.org
        - mpv.io
        - discord.sex
        - discord.dev
        - ss64.com
        - tom-select.js.org
        - docs.qmk.fm
        - discord.com
        required: false
        type: choice
